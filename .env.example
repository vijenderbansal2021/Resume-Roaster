# For Google AI Studio (production):
# Configure GEMINI_API_KEY via the AI Studio Secrets panel
# It will be automatically injected at runtime

# For local development:
# Create a .env file (not tracked in Git) with:
# GEMINI_API_KEY="your-test-key"

APP_URL="https://resume-roster.ai.studio"